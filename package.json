{
  "name": "geek-command",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "install": "pushd browser_client && npm install && popd && npm install",
    "start": "nodemon ./bin/www",
    "build": "node ./build.js",
    "deploy": "sls deploy --stage %STAGE% --region %REGION%",
    "remove": "sls remove --stage %STAGE% --region %REGION%",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "aws-serverless-express": "^3.0.2",
    "express": "~4.15.5",
    "js-yaml": "^3.10.0",
    "node-fetch": "^1.7.3",
    "oauth": "^0.9.15",
    "serve-favicon": "~2.4.5"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^4.1.0",
    "serverless": "^1.25.0"
  }
}
